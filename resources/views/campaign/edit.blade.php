@extends('layout')
@section('content')
    <div class="row">
        <div class="col-lg-12 margin-tb">
            <div class="pull-left">
                <h2>Edit Post</h2>
            </div>
        </div>
    </div>
    @if ($errors->any())
        <div class="alert alert-danger">
            <strong>Whoops!</strong> There were some problems with your input.<br><br>
            <ul>
                @foreach ($errors->all() as $error)
                    <li>{{ $error }}</li>
                @endforeach
            </ul>
        </div>
    @endif
    <form action="{{ route('campaigns.update',$campaign->id) }}" method="POST" enctype="multipart/form-data"> 
        @csrf
        @method('PUT')
         <div class="row">
            <div class="col-xs-12 col-sm-12 col-md-12">
                <div class="form-group">
                    <strong>Name:</strong>
                    <input type="text" name="name" value="{{ $campaign->name }}" class="form-control" placeholder="Campaign Name">
                </div>
            </div>
            <div class="col-xs-12 col-sm-12 col-md-12">
                <div class="form-group">
                    <strong>Date From:</strong>
                    <input type="date" name="date_from" value="{{ $campaign->date_from }}" class="form-control" placeholder="Date From">
                </div>
            </div>
            <div class="col-xs-12 col-sm-12 col-md-12">
                <div class="form-group">
                    <strong>Date To:</strong>
                    <input type="date" name="date_to" value="{{ $campaign->date_to }}" class="form-control" placeholder="Date To">
                </div>
            </div>
            <div class="col-xs-12 col-sm-12 col-md-12">
                <div class="form-group">
                    <strong>Total Budget:</strong>
                    <input type="number" name="total_budget" value="{{ $campaign->total_budget }}" class="form-control" placeholder="Total Budget">
                </div>
            </div>
            <div class="col-xs-12 col-sm-12 col-md-12">
                <div class="form-group">
                    <strong>Daily Budget:</strong>
                    <input type="number" name="daily_budget" value="{{ $campaign->daily_budget }}" class="form-control" placeholder="Daily Budget">
                </div>
            </div>
            <div class="col-xs-12 col-sm-12 col-md-12">
                <div class="form-group">
                    <strong>Image:</strong>
                    <input type="file" name="image" class="form-control" placeholder="Campaign Image">
                    <img src="/uploads/{{ $campaign->image }}" width="200px">
                </div>
            </div>
            <div class="col-xs-12 col-sm-12 col-md-12 text-center">
              <button type="submit" class="btn btn-primary">Update campaign</button>
            </div>
        </div>
    </form>
@endsection